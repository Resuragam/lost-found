/**app.wxss**/
page {
  background-color: #fcfcfc;
}

.tabbar-page-container {
  padding: 0 32rpx calc(112rpx + env(safe-area-inset-bottom)) 32rpx;
}
.page-container {
  padding: 0 32rpx calc(40rpx + env(safe-area-inset-bottom));
}
